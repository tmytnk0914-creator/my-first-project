# CLAUDE.md

このファイルはClaude Codeがこのプロジェクトで作業する際のガイドラインです。

## 基本ワークフロー

作業は必ず以下の順序で進める:

1. **Research** - コードベースを理解する
2. **Plan** - 実装前に計画を立てる（plan modeを活用）
3. **Execute** - 計画に従って実装する
4. **Review** - 変更内容を確認する
5. **Ship** - コミット・プッシュする

## コミット規約

- **ファイルごとに個別コミット**を作成する（複数ファイルをまとめない）
- コミットメッセージは変更内容を具体的に記述する
- 例: `Add user authentication endpoint` / `Fix null pointer in payment handler`

## PR ガイドライン

- PRは小さく保つ（目安: 変更行数 ~120行）
- スカッシュマージを使用してクリーンな履歴を維持する
- PRの説明には「何を・なぜ」を明記する

## コンテキスト管理

- コンテキスト使用率が **50%前後** になったら手動でコンパクションを実行する
- 長いセッションでは新しいコンテキストで作業を再開する

## 計画モードの使い方

複雑なタスクは必ずplan modeから開始する:
- ユーザーへのヒアリングを行い要件を明確化する
- フェーズ分けされた段階的な計画を立てる
- 計画をレビューしてから実行に移る

## コード品質

- 変更後は `/simplify` スキルを使ってリファクタリングを確認する
- テスト駆動開発（TDD）を優先する: テストを先に書く
- セキュリティ上の問題（インジェクション・XSS等）には常に注意する

## プロンプティングのコツ

- Claudeに対して「より良い解決策があれば提案して」と促す
- 実装の細かい手順より **目標・制約・品質基準** を伝える
- 想定外の問題に遭遇したら作業を止めて確認を求める

## ファイル構成の原則

- このCLAUDE.mdは **200行以内** に維持する
- 追加ルールは `.claude/rules/` ディレクトリに分割して管理する
- `@path` 構文でルールファイルをインポートできる

## 禁止事項

- テストをスキップしてコミットしない（`--no-verify` 使用禁止）
- 未確認のまま破壊的な操作（`git reset --hard`, `rm -rf` 等）を実行しない
- 承認なしに main/master へ直接プッシュしない
